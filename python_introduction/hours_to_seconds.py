# A program to convert hours to seconds.
hours = 2
seconds = hours * 3600
print(f"{hours} hours(s) is {seconds} seconds.")


#  hours_to_seconds.py doesn't
# contain: ["seconds = hours * 3600"]
# - hours_to_seconds.py doesn't contain: ["seconds=hours*3600"]
# - hours_to_seconds.py doesn't contain: ["seconds = 3600 * hours"]

# - hours_to_seconds.py doesn't contain: ["seconds=3600*hours"]
